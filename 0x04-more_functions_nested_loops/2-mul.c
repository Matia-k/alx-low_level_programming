#include "main.h"
/**
  *mul - mutltiplies two integers
  *@a: one of the integers
  *@b: the other integer
  *Return: the product of the two ints
*/
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
